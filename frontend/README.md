# FreelanceFlow Frontend

Vue 3 SPA for FreelanceFlow.

## Run

```bash
npm install
npm run dev
```

Set `VITE_API_URL=http://localhost:8000` in `.env` if the Laravel API uses the default local port.

## Build

```bash
npm run build
```
